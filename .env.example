# =============================================================================
# Azure SWA Fullstack Starter - Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your deployment.
# For local development, also create app/api/local.settings.json
#
# IMPORTANT: Never commit .env or local.settings.json to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Application Branding
# -----------------------------------------------------------------------------
# These values customise the look and feel of your application

VITE_APP_NAME="My App"
VITE_APP_DESCRIPTION="A modern web application"
VITE_APP_TAGLINE="Welcome to your application"

# Path to your logo (place in app/public/)
VITE_LOGO_PATH="/logo.svg"

# -----------------------------------------------------------------------------
# Owner/Company Information (optional)
# -----------------------------------------------------------------------------
# Displayed in the application footer if VITE_SHOW_OWNER_FOOTER is true

VITE_OWNER_NAME=""
VITE_OWNER_COMPANY=""
VITE_SHOW_OWNER_FOOTER="false"

# -----------------------------------------------------------------------------
# Storage Configuration
# -----------------------------------------------------------------------------
# Prefix for localStorage keys (prevents collisions with other apps)

VITE_STORAGE_PREFIX="app"

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Base URL for API calls (default works with Azure SWA proxy)

VITE_API_BASE_URL="/api"

# -----------------------------------------------------------------------------
# Backend Configuration (for api/local.settings.json)
# -----------------------------------------------------------------------------
# These are documented here but should be set in local.settings.json for local
# development, and in Azure App Settings for production.

# COSMOS_DB_CONNECTION_STRING - Your Azure Cosmos DB connection string
# COSMOS_DB_DATABASE_NAME     - Database name (default: "appdb")
# JWT_SECRET                  - Secret key for JWT token signing (min 32 chars)
